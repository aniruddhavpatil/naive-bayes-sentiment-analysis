To run the program use:
python3 classify.py

To run on a custom dataset, uncomment the line under CUSTOM_DATASET and put CUSTOM_DATASET='filename of custom dataset'

The program runs both the experiments on all the datasets.
